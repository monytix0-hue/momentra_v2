import SwiftUI

/// Figma bottom-sheet host tokens for Business Create + Setup (658:9451+, 692:34736+).
enum BusinessSheetTheme {
    static let bg = Color(hex: "#0C0F15")
    static let card = Color(hex: "#161B26")
    static let border = Color(hex: "#1E293B")
    static let accent = Color(hex: "#818CF8")
}
