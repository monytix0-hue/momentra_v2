package com.example.momentra.ui.shell.empty.group

/** Nested Create flow for Group (chooser → section setup wizards). */
enum class GroupCreatePhase {
    CHOOSER,
    EXPERIENCE_SETUP,
    PURCHASE_SETUP,
    LIVING_SETUP,
}
